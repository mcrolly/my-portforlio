version: 0.1

phases:
    build:
      command:
        - npm install
        - npm run webpack
artifacts:
  files:
    - index.html
    - react.html
    - favicon.ico
    - styles/*
    - images/*
    - dist/bundle.js
    - babel.html
